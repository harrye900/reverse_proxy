apiVersion: v1
kind: Service
metadata:
  name: simple-lb
  namespace: ecommerce
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    name: frontend
  - port: 5000
    targetPort: 5000
    name: backend
  selector:
    app: frontend